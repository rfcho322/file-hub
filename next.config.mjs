/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname: "scintillating-snake-679.convex.cloud",
            }
        ],
    },
};

export default nextConfig;
