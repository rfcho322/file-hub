import { FilesDisplay } from "@/app/_components/FileDisplay";

export default function FavoritesPage () {
    return (
        <div>
            <FilesDisplay title="Your Favorites"/>
        </div>
    );
}